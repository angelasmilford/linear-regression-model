print("Hello, World!")

print(2)
print(4)
print(6)
print(11)

print(5 + 5)

plot(1:4)

# This is a comment
# R does not have a syntax for multi-line comments, so
# for we can just insert a # to create multi-line comments

# When outputting code in R, the "print()" function doesn't have to be used
# However, for VS code, it is required to use "print()" to see the output in the console
# When outputting the result inside an R expression (inside curly braces), the print() function must be used